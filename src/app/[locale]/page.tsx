'use client';

import {WorkspaceDashboard} from '@/components/WorkspaceDashboard'; // 工作区仪表盘组件 / Workspace dashboard component

// 应用首页，挂载工作区仪表盘 / Application home page mounting the workspace dashboard
export default function HomePage() {
  return <WorkspaceDashboard />; // 渲染主控制面板 / Render main control panel
}

